doctype html
html
  head
    title status
    link(rel='stylesheet', href='/stylesheets/style.css')
    script(src='https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js') 
    script.
      window.onload = () => {setTimeout(function() {window.location.href ='#{url}'+'?error=#{status}';}, 3000);};
  body
    h1= status!=="Logging Out"?status:''
    h2= status==="Logging Out"?status:''
